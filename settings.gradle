pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url 'https://jfrog.ghtklab.com/artifactory/ghtkDev'
            isAllowInsecureProtocol()
            credentials {
                it.username("hieuna7")
                it.password("Ghtk@2022")
            }
        }
    }
}
rootProject.name = "GCabinet"
include ':app'
include ':serialport'
include ':network'
